# Render deployment configuration
services:
  - type: web
    name: ml-model-2025-10-04T11-43-39
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python load_model.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
